Temporarily archived...

---
# Tuplite
A python NamedTuple based interface to SQLite (or something like that)

See `tuplite.py`

Currently in very early dev (part of a different project), and shouldn't be used.
