"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = handleCompile;
var _slash = _interopRequireDefault(require("slash"));
var _path = require("path");
var _constants = require("./constants");
var _util = require("./util");
var _compile = require("./compile");
var _options = require("./options");
async function handleCompile(opts) {
    var _outFileExtension;
    const dest = (0, _util).getDest(opts.filename, opts.outDir, opts.cliOptions.stripLeadingPaths, `.${(_outFileExtension = opts.outFileExtension) !== null && _outFileExtension !== void 0 ? _outFileExtension : _options.DEFAULT_OUT_FILE_EXTENSION}`);
    const sourceFileName = (0, _slash).default((0, _path).relative((0, _path).dirname(dest), opts.filename));
    const options = {
        ...opts.swcOptions,
        sourceFileName
    };
    const result = await (0, _util).compile(opts.filename, options, opts.sync, dest);
    if (result) {
        await (0, _compile).outputResult(result, opts.filename, dest, options);
        return _constants.CompileStatus.Compiled;
    } else {
        return _constants.CompileStatus.Omitted;
    }
}
function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
